<h1>Your search of : {{ searchTerm }}</h1>
<hr>

{{#if hasContent}}
    {{#each img}}
        <div>
            <h5>{{ this.title }}</h5>
            <img src={{this.url}} alt="">
        </div>
        
    {{/each}} 
{{else}}
    <h3> Your search returns no result.</h3>
{{/if}}